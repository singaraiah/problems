package examples.scala

object test extends App {

  val arr = "2[aa]3[b]".toCharArray

  def decodeStr(arr: Array[Char]) = {

    val resStr = scala.collection.mutable.ListBuffer[Char]()
    for(i <- 0 until arr.length) {

    }

  }

  decodeStr(arr)
}
